# TODO

## 开发方向

将当前的单次点击脚本升级为可在 macOS 上运行的桌面自动化工具：用户可以查看和获取鼠标坐标，编排“移动、点击”步骤，并在窗口中控制和查看执行过程。

后续方向：

- 支持步骤配置的保存与载入。
- 支持循环执行、点击类型和更丰富的等待条件。
- 增加目标窗口或图像识别，降低固定坐标误点击的风险。
- 使用 PyInstaller 打包为 macOS `.app`。

## Alpha 版

### 1. 桌面窗口

- [ ] 使用 Tkinter 创建 macOS 可操作窗口。
- [ ] 显示当前鼠标的实时 `X / Y` 坐标。
- [ ] 提供“获取当前坐标”按钮，自动填入步骤表单。
- [ ] 检查并提示 macOS 辅助功能权限。

### 2. 操作步骤

- [ ] 支持添加“移动”和“点击”两种步骤。
- [ ] 每个步骤包含：操作类型、`X`、`Y`、执行前等待时间。
- [ ] “移动”只移动鼠标；“点击”移动到坐标后单击。
- [ ] 支持编辑、删除、上移和下移步骤。
- [ ] 校验坐标、等待时间以及空步骤。

### 3. 执行控制

- [ ] 提供开始、暂停、继续和停止按钮。
- [ ] 开始前显示 3 秒倒计时。
- [ ] 执行期间锁定步骤编辑，结束后恢复。
- [ ] 保留 PyAutoGUI 左上角紧急停止机制。
- [ ] 捕获执行异常并在窗口中显示原因。

### 4. 状态与统计

- [ ] 显示当前状态：待机、倒计时、运行、暂停、完成、停止或错误。
- [ ] 显示当前执行到第几步，例如 `2 / 5`。
- [ ] 在步骤列表中高亮当前步骤。
- [ ] 显示本次运行经过时间。
- [ ] 显示本次已完成的操作次数。

### 5. Alpha 验收

- [ ] 可以创建一组包含移动和点击的步骤并按顺序执行。
- [ ] 暂停、继续和停止不会导致窗口卡死。
- [ ] 计时、操作次数和当前步骤显示正确。
- [ ] 非法坐标或操作失败时不会继续盲目点击。
- [ ] 在单显示器和外接显示器环境下完成一次基础测试。
