# TODO

## 开发方向

将当前的单次点击脚本升级为可在 macOS 上运行的桌面自动化工具：用户可以查看和获取鼠标坐标，编排“移动、点击、双击”步骤，并在窗口中控制和查看执行过程。

## 下一阶段

### 1. 流程能力

- [ ] 支持整套流程循环执行指定次数或无限循环，且可随时停止。
- [ ] 支持为单个步骤设置重复次数。
- [ ] 支持随机等待时间，使操作间隔更加自然。
- [ ] 支持步骤跳转、条件分支和失败重试。
- [ ] 支持等待指定条件成立后再继续执行。
- [ ] 支持设置单步执行超时，并在超时后停止或进入失败处理。
- [ ] 支持将工作流保存为 JSON 文件并从 JSON 文件载入。

### 2. 提高坐标可靠性

- [ ] 支持使用相对于目标窗口的坐标。
- [ ] 支持使用相对于屏幕宽高的百分比坐标。
- [ ] 支持选择并识别多个显示器。
- [ ] 支持在执行前显示目标位置或区域的截图预览。
- [ ] 支持点击前检查目标像素颜色，不匹配时停止或进入失败处理。
- [ ] 支持限制鼠标只能在指定区域内操作。

### 3. 其他方向

- [ ] 增加目标窗口或图像识别，进一步降低固定坐标误点击的风险。
- [ ] 使用 PyInstaller 打包为 macOS `.app`。

## Alpha 版

### 1. 桌面窗口

- [x] 使用 Tkinter 创建 macOS 可操作窗口。
- [x] 显示当前鼠标的实时 `X / Y` 坐标。
- [x] 提供“获取当前坐标”按钮，自动填入步骤表单。
- [x] 检查并提示 macOS 辅助功能权限。

### 2. 操作步骤

- [x] 支持添加“移动”“点击”和“双击”三种步骤。
- [x] 每个步骤包含：操作类型、`X`、`Y`、执行前等待时间。
- [x] “移动”只移动鼠标；“点击”移动到坐标后单击；“双击”在同一步内连续点击两次。
- [x] 支持编辑、删除、上移和下移步骤。
- [x] 校验坐标、等待时间以及空步骤。

### 3. 执行控制

- [x] 提供开始、暂停、继续和停止按钮。
- [x] 开始前显示 3 秒倒计时。
- [x] 执行期间锁定步骤编辑，结束后恢复。
- [x] 保留 PyAutoGUI 左上角紧急停止机制。
- [x] 捕获执行异常并在窗口中显示原因。

### 4. 状态与统计

- [x] 显示当前状态：待机、倒计时、运行、暂停、完成、停止或错误。
- [x] 显示当前执行到第几步，例如 `2 / 5`。
- [x] 在步骤列表中高亮当前步骤。
- [x] 显示本次运行经过时间。
- [x] 显示本次已完成的操作次数。

### 5. Alpha 验收

- [x] 可以创建一组包含移动和点击的步骤并按顺序执行。
- [x] 暂停、继续和停止不会导致窗口卡死。
- [x] 计时、操作次数和当前步骤显示正确。
- [x] 非法坐标或操作失败时不会继续盲目点击。
- [x] 完成单显示器基础测试，并用负坐标自动测试覆盖多显示器坐标兼容。（外接显示器实机测试后续补充）
